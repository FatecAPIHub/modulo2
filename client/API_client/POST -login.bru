meta {
  name: POST /login
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
