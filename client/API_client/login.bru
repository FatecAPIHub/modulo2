meta {
  name: /login
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "novouser",
    "password": "mangas"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
